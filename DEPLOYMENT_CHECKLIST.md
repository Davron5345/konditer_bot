# 🎯 ЧЕКЛИСТ ДЕПЛОЯ НА RAILWAY

## ✅ Готовность проекта

- [x] Все зависимости установлены (requirements.txt)
- [x] Конфигурация настроена (config.py + Products)
- [x] База данных работает (database.py + get_today_stats)
- [x] Часовой пояс Asia/Tashkent настроен
- [x] Точка входа создана (start.py)
- [x] Procfile для Railway создан
- [x] runtime.txt с версией Python создан
- [x] README.md с полной документацией

## 📋 ЧТО ДЕЛАТЬ ДАЛЬШЕ

### 1. Закоммитьте изменения в Git

```powershell
git status
git add .
git commit -m "Setup konditer_bot for Railway: Products, timezone, all configs"
git push origin main
```

### 2. Деплой на Railway

1. Откройте: https://railway.app/new
2. Выберите: **"Deploy from GitHub"**
3. Подключите репозиторий: `Davron5345/konditer_bot`
4. Railway автоматически обнаружит `Procfile` и запустит проект

### 3. Настройте переменные окружения в Railway

В Railway Dashboard → Settings → Variables добавьте:

```
BOT_TOKEN=8307242308:AAH9ZJsawsWulFrdeIHFPI1-bKf6NN4FOdw
CHANNEL_ID=-1003367790885
ADMIN_IDS=6779317446
API_SECRET_KEY=your-super-secret-key-for-production
DATABASE_URL=(будет автоматически после подключения PostgreSQL)
```

### 4. Подключите PostgreSQL (ВАЖНО!)

1. В Railway: **Add Plugin** → **PostgreSQL**
2. После создания скопируйте `DATABASE_URL` из переменных PostgreSQL
3. Добавьте `DATABASE_URL` в переменные проекта

**Примечание:** SQLite не сохраняется между деплоями на Railway!

### 5. Проверьте логи

Railway Dashboard → Deploys → View Logs

Должны увидеть:
```
Bot token: 8307242308...
Бот запускается...
 * Running on http://0.0.0.0:XXXX
```

### 6. Тестирование

- Telegram: Отправьте `/start` боту
- Админ-панель: Откройте `your-app.railway.app/admin`
- Создайте тестовый заказ через бота
- Проверьте уведомление в канале -1003367790885

## 🧪 ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ (опционально)

### Запуск локально:

```powershell
# Windows:
run_local.bat

# Или напрямую:
python start.py
```

### Проверка настройки:

```powershell
python test_setup.py
```

### Доступ:
- Админ-панель: http://localhost:8080/admin
- Telegram бот: отправьте /start

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Токен бота**: Уже настроен в .env (8307242308:...)
2. **Канал**: Уже настроен (-1003367790885)
3. **Админ ID**: Уже настроен (6779317446)
4. **База данных**: Используйте PostgreSQL на Railway!
5. **Часовой пояс**: Все времена в Asia/Tashkent (UTC+5)

## 🎯 СЛЕДУЮЩИЕ ШАГИ

### Сейчас сделайте:

1. `git add .`
2. `git commit -m "Ready for Railway"`
3. `git push origin main`
4. Откройте https://railway.app/new
5. Deploy from GitHub → Davron5345/konditer_bot
6. Добавьте переменные окружения (см. выше)
7. Подключите PostgreSQL
8. Проверьте логи и протестируйте бота

### После деплоя:

- Проверьте бота в Telegram
- Откройте админ-панель
- Создайте тестовый заказ
- Проверьте статистику в админке

## 📞 ПОДДЕРЖКА

Если что-то не работает:
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все переменные окружения заданы
3. Проверьте, что бот добавлен как админ в канал
4. Убедитесь, что PostgreSQL подключен

---

**🚀 ВСЁ ГОТОВО К ДЕПЛОЮ!**
